// Minimal demo for the LOADPC instruction.
// loadpc(x) stores the PC of the *next* instruction (PC+4) into x.

u32 x;

start:
loadpc(x);
// Keep the program doing something so there's a next instruction.
wait_pixel(640);

goto start;
